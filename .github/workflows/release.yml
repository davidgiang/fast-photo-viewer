name: Build and Release

on:
  push:
    branches: [master]
    tags: ['v*']
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version bump type'
        required: true
        default: 'patch'
        type: choice
        options: [patch, minor, major]

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  # BtbN rotates assets off the rolling `latest` release, so pin an immutable
  # autobuild tag. ffmpeg-the-third 2.0 only binds libavcodec <= 61 (FFmpeg 7.1)
  # -- do NOT bump this to 8.x/9.x without upgrading the crate first.
  FFMPEG_RELEASE: autobuild-2026-07-31-14-10
  FFMPEG_ASSET: ffmpeg-n7.1.5-12-g1fdbca85aa-win64-gpl-shared-7.1.zip

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Guard - dispatch only from master
        if: github.event_name == 'workflow_dispatch' && github.ref_name != 'master'
        shell: bash
        run: |
          echo "::error::workflow_dispatch releases must run on master, got '${{ github.ref_name }}'"
          exit 1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Compute version
        id: version
        shell: bash
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            BUMP="${{ inputs.bump }}"
            BUMP="${BUMP:-patch}"
            cargo install cargo-edit --locked
            cargo set-version --bump "$BUMP"
            VERSION=$(cargo pkgid | sed -E 's/.*[#@]([0-9]+\.[0-9]+\.[0-9]+).*/\1/')
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Releasing v$VERSION"

      - name: Download FFmpeg shared build
        shell: bash
        run: |
          set -euo pipefail
          curl -L --fail --retry 5 --retry-delay 5 -o ffmpeg.zip \
            "https://github.com/BtbN/FFmpeg-Builds/releases/download/${FFMPEG_RELEASE}/${FFMPEG_ASSET}"
          unzip -q ffmpeg.zip -d ffmpeg7-raw
          # Archive contains a single versioned top-level dir; normalise the name
          # so later steps never have to hardcode the build hash.
          mv ffmpeg7-raw/*/ ffmpeg7
          test -d ffmpeg7/bin || { echo "::error::unexpected FFmpeg archive layout"; ls -R ffmpeg7 | head -20; exit 1; }
          echo "FFMPEG_DIR=${GITHUB_WORKSPACE}/ffmpeg7" >> "$GITHUB_ENV"

      - name: Build release binary
        shell: bash
        run: cargo build --release

      - name: Stage DLLs for installer
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dlls
          cp ffmpeg7/bin/*.dll dlls/

      - name: Install Inno Setup
        shell: powershell
        run: choco install innosetup -y --no-progress

      - name: Build installer
        shell: cmd
        run: |
          "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" /dMyAppVersion=${{ steps.version.outputs.VERSION }} installer\setup.iss

      - name: Commit bump and push tag
        if: github.ref_type != 'tag'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Cargo.toml Cargo.lock
          git commit -m "chore: release v${{ steps.version.outputs.VERSION }}"
          git tag "v${{ steps.version.outputs.VERSION }}"
          git push origin "HEAD:${{ github.ref_name }}"
          git push origin "v${{ steps.version.outputs.VERSION }}"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ steps.version.outputs.VERSION }}"
          name: "Fast Photo Viewer v${{ steps.version.outputs.VERSION }}"
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            output/FastPhotoViewer-*-setup.exe
          body: |
            ### Installation
            1. Download `FastPhotoViewer-${{ steps.version.outputs.VERSION }}-setup.exe`
            2. Run the installer
            3. Choose which file types to associate (images checked by default, videos optional)
            4. To set as your default viewer, go to **Windows Settings > Default Apps** and search for **Fast Photo Viewer**

            ### Supported Formats
            **Images:** jpg, jpeg, png, bmp, webp, gif, tiff, ico, svg
            **Videos:** mp4, mkv, avi, mov, wmv, flv, webm, m4v, mpg, mpeg, 3gp, ogv, vob

            ---
